import WorksView from './WorksView';

export const metadata = {
  title: 'DESIGN ARK // Works & Specifications',
  description:
    'Selected residential, workspace and commercial projects from Design Ark — filterable by category, with location, scope and material specifications for each.',
};

export default function Page() {
  return <WorksView />;
}
